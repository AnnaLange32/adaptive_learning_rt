import numpy as np

# Specify the file path
file_path = 'behaviors.npy'

# Load the array from the file
loaded_array = np.load(file_path)

# Now 'loaded_array' contains the data from the saved .npy file
print(loaded_array)

# Specify the file path
file_path = 'placements.npy'

# Load the array from the file
loaded_array = np.load(file_path)

# Now 'loaded_array' contains the data from the saved .npy file
print(loaded_array)

# Specify the file path
file_path = 'performance.npy'

# Load the array from the file
loaded_array = np.load(file_path)

# Now 'loaded_array' contains the data from the saved .npy file
print(loaded_array)


